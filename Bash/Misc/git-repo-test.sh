#!/usr/bin/env bash

url="https://raw.githubusercontent.com/slyfox1186/script-repo/main/Bash/Misc/source-git-repo-version.sh"

echo -n "ansible: "
curl -sSL "$url" | bash -s "https://github.com/ansible/ansible.git"
echo -n "beats: "
curl -sSL "$url" | bash -s "https://github.com/elastic/beats.git"
echo -n "cassandra: "
curl -sSL "$url" | bash -s "https://github.com/apache/cassandra.git"
echo -n "chef: "
curl -sSL "$url" | bash -s "https://github.com/chef/chef.git"
echo -n "consul: "
curl -sSL "$url" | bash -s "https://github.com/consul/consul.git"
echo -n "cpython: "
curl -sSL "$url" | bash -s "https://github.com/python/cpython.git"
echo -n "curl: "
curl -sSL "$url" | bash -s "https://github.com/curl/curl.git"
echo -n "docker-ce: "
curl -sSL "$url" | bash -s "https://github.com/docker/docker-ce.git"
echo -n "drill: "
curl -sSL "$url" | bash -s "https://github.com/apache/drill.git"
echo -n "elasticsearch: "
curl -sSL "$url" | bash -s "https://github.com/elastic/elasticsearch.git"
echo -n "flink: "
curl -sSL "$url" | bash -s "https://github.com/apache/flink.git"
echo -n "gcc: "
curl -sSL "$url" | bash -s "https://github.com/gcc-mirror/gcc.git"
echo -n "git: "
curl -sSL "$url" | bash -s "https://github.com/git/git.git"
echo -n "go: "
curl -sSL "$url" | bash -s "https://github.com/golang/go.git"
echo -n "grafana: "
curl -sSL "$url" | bash -s "https://github.com/grafana/grafana.git"
echo -n "hadoop: "
curl -sSL "$url" | bash -s "https://github.com/apache/hadoop.git"
echo -n "hbase: "
curl -sSL "$url" | bash -s "https://github.com/apache/hbase.git"
echo -n "hive: "
curl -sSL "$url" | bash -s "https://github.com/apache/hive.git"
echo -n "httpd: "
curl -sSL "$url" | bash -s "https://github.com/apache/httpd.git"
echo -n "influxdb: "
curl -sSL "$url" | bash -s "https://github.com/influxdata/influxdb.git"
echo -n "kafka: "
curl -sSL "$url" | bash -s "https://github.com/apache/kafka.git"
echo -n "kubernetes: "
curl -sSL "$url" | bash -s "https://github.com/kubernetes/kubernetes.git"
echo -n "libpng: "
curl -sSL "$url" | bash -s "https://github.com/pnggroup/libpng.git"
echo -n "linux: "
curl -sSL "$url" | bash -s "https://github.com/torvalds/linux.git"
echo -n "llvm: "
curl -sSL "$url" | bash -s "https://github.com/llvm/llvm-project.git"
echo -n "logstash: "
curl -sSL "$url" | bash -s "https://github.com/elastic/logstash.git"
echo -n "lucene: "
curl -sSL "$url" | bash -s "https://github.com/apache/lucene.git"
echo -n "moby: "
curl -sSL "$url" | bash -s "https://github.com/moby/moby.git"
echo -n "mongodb: "
curl -sSL "$url" | bash -s "https://github.com/mongodb/mongo.git"
echo -n "nginx: "
curl -sSL "$url" | bash -s "https://github.com/nginx/nginx.git"
echo -n "nifi: "
curl -sSL "$url" | bash -s "https://github.com/apache/nifi.git"
echo -n "node: "
curl -sSL "$url" | bash -s "https://github.com/nodejs/node.git"
echo -n "openssl: "
curl -sSL "$url" | bash -s "https://github.com/openssl/openssl.git"
echo -n "php: "
curl -sSL "$url" | bash -s "https://github.com/php/php-src.git"
echo -n "postgres: "
curl -sSL "$url" | bash -s "https://github.com/postgres/postgres.git"
echo -n "prometheus: "
curl -sSL "$url" | bash -s "https://github.com/prometheus/prometheus.git"
echo -n "puppet: "
curl -sSL "$url" | bash -s "https://github.com/puppetlabs/puppet.git"
echo -n "redis: "
curl -sSL "$url" | bash -s "https://github.com/redis/redis.git"
echo -n "ruby: "
curl -sSL "$url" | bash -s "https://github.com/ruby/ruby.git"
echo -n "rust: "
curl -sSL "$url" | bash -s "https://github.com/rust-lang/rust.git"
echo -n "salt: "
curl -sSL "$url" | bash -s "https://github.com/saltstack/salt.git"
echo -n "spark: "
curl -sSL "$url" | bash -s "https://github.com/apache/spark.git"
echo -n "sqlite: "
curl -sSL "$url" | bash -s "https://github.com/sqlite/sqlite.git"
echo -n "terraform-provider-aws: "
curl -sSL "$url" | bash -s "https://github.com/terraform-providers/terraform-provider-aws.git"
echo -n "vault: "
curl -sSL "$url" | bash -s "https://github.com/hashicorp/vault.git"
echo -n "vim: "
curl -sSL "$url" | bash -s "https://github.com/vim/vim.git"
echo -n "zookeeper: "
curl -sSL "$url" | bash -s "https://github.com/apache/zookeeper.git"
